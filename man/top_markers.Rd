% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/queries.R
\name{top_markers}
\alias{top_markers}
\title{Get top marker genes for a Leiden cluster within a tissue}
\usage{
top_markers(markers, tissue, cluster, top_n = 20, min_log2fc = 0.25)
}
\arguments{
\item{markers}{A data frame or tibble containing marker results. Must include
the columns \code{tissue}, \code{leiden}, \code{avg_log2FC}, and \code{p_val_adj}. The table is
expected to have one row per marker feature/gene per tissue-cluster
comparison.}

\item{tissue}{A single character string giving the tissue to filter, for
example \code{"liver"} or \code{"heart"}. The value must be present in
\code{markers$tissue}.}

\item{cluster}{A single Leiden cluster identifier. Can be character or
numeric. Internally, both \code{cluster} and \code{markers$leiden} are compared as
character values.}

\item{top_n}{Integer. Maximum number of marker rows to return. Defaults to
\code{20}.}

\item{min_log2fc}{Numeric. Minimum \code{avg_log2FC} required for a marker to be
retained. Defaults to \code{0.25}.}
}
\value{
A tibble containing up to \code{top_n} marker rows for the requested tissue
and cluster, filtered to \code{avg_log2FC >= min_log2fc} and \code{p_val_adj < 0.05},
sorted by decreasing \code{avg_log2FC}.
}
\description{
Filter a marker table to one tissue and one Leiden cluster, keep statistically
significant positive markers, and return the top markers ranked by decreasing
average log2 fold change.
}
\details{
This function is intended for marker tables generated from Leiden-cluster
differential expression analyses. Positive \code{avg_log2FC} values are interpreted
as enrichment in the target cluster relative to the comparison group. Rows with
missing values in \code{avg_log2FC} or \code{p_val_adj} are implicitly removed by the
filtering conditions.
}
\examples{
markers <- tibble::tibble(
  tissue = c("liver", "liver", "liver", "heart"),
  leiden = c("1", "1", "2", "1"),
  gene = c("GeneA", "GeneB", "GeneC", "GeneD"),
  avg_log2FC = c(1.2, 0.8, 0.4, 1.5),
  p_val_adj = c(0.001, 0.02, 0.04, 0.01)
)

top_markers(markers, tissue = "liver", cluster = "1")
top_markers(markers, tissue = "liver", cluster = 1, top_n = 1)

}
